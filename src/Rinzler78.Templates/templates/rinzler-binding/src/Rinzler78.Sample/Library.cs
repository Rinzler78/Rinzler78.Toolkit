namespace Rinzler78.Sample;

/// <summary>
/// The entry point of this library. Replace it: it exists so that the repository
/// compiles, documents, packs and tests something real from its first commit.
/// </summary>
public static class Library
{
    /// <summary>Gets the simple name of the assembly this type belongs to.</summary>
    public static string Name => typeof(Library).Assembly.GetName().Name!;
}
