# Templates

`rinzler-lib`, `rinzler-binding` and `rinzler-app` will live here, each expanding a
complete harness: the seventeen vendored skills, both agent entry points, the hooks,
the pre-commit configuration, the toolchain manifests, the workflows, the labels,
the issue templates and the script facade.

They are not written yet. The check in `scripts/hooks/harness-regenerates.sh` reports
that it has nothing to compare rather than claiming a success it did not earn; it
starts verifying the moment a `.template.config` directory appears here.
